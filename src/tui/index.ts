/**
 * Orquesta Terminal — TUI Mode (coming soon)
 *
 * This will be the terminal-native version using Ink (React for CLI).
 * Same architecture as the web version but rendered in the terminal directly.
 *
 * Usage:
 *   npx orquesta-terminal
 *   # or
 *   orquesta-terminal --backend http://localhost:3000
 */

console.log(`
  ╔═══════════════════════════════════╗
  ║   Orquesta Terminal (TUI mode)   ║
  ╠═══════════════════════════════════╣
  ║                                   ║
  ║   TUI mode coming soon.           ║
  ║                                   ║
  ║   For now, use web mode:          ║
  ║   $ npm run dev                   ║
  ║   → http://localhost:4000         ║
  ║                                   ║
  ╚═══════════════════════════════════╝
`)
