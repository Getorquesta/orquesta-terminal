{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Least-privilege IPC surface for the main window: the webview may call the app's own commands (PTY, cloud, fs helpers) and the clipboard, nothing else. File access happens inside those Rust commands, so the fs, dialog and shell plugins are deliberately not exposed over IPC.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "clipboard-manager:allow-read-text",
    "clipboard-manager:allow-write-text"
  ]
}
