# Orquesta Terminal Configuration

# Backend URL — where the self-hosted Orquesta OSS is running
NEXT_PUBLIC_BACKEND_URL=http://localhost:3000

# Or connect directly to hosted (no self-hosted needed)
# NEXT_PUBLIC_BACKEND_URL=https://getorquesta.com
