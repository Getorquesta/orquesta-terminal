# @xterm/addon-web-links@0.11 declares peer @xterm/xterm@^5 but the project uses
# @xterm/xterm@6 → npm ci fails with ERESOLVE. The addons are API-compatible, so
# allow the legacy peer resolution (also unblocks `npm ci` in CI).
legacy-peer-deps=true
